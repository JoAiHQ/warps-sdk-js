---
"@joai/warps-mcp": minor
---

Add `AppMcpConfig` type and `buildAppMcpResourceMeta` helper for MCP Apps resource metadata. Applies secure defaults (CSP, permissions, border) for the `meta.ui` block expected by MCP Apps clients.
